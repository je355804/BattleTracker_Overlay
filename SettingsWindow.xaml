<Window x:Class="BattleTrackerOverlay.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Stat Settings"
        SizeToContent="WidthAndHeight"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner"
        MinWidth="520"
        MinHeight="360"
        Background="#FF1E1E1E"
        Foreground="White">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Choose which stats appear in the overlay."
                   FontSize="14"
                   Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
            <TextBlock Text="Overlay opacity" FontSize="12" VerticalAlignment="Center"/>
            <Slider Width="200"
                    Margin="12,0,0,0"
                    Minimum="0.2"
                    Maximum="1.0"
                    SmallChange="0.05"
                    LargeChange="0.1"
                    Value="{Binding OverlayOpacity, Mode=TwoWay}"/>
            <TextBlock Margin="12,0,0,0"
                       VerticalAlignment="Center"
                       Text="{Binding OverlayOpacity, StringFormat={}{0:P0}}"/>
        </StackPanel>

        <TabControl Grid.Row="2"
                    ItemsSource="{Binding Scopes}"
                    SelectedItem="{Binding SelectedScope, Mode=TwoWay}"
                    Margin="0,8,0,0"
                    Background="Transparent">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Foreground" Value="#FFEEEEEE"/>
                    <Setter Property="Padding" Value="18,8"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="MinWidth" Value="140"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="Bd"
                                        Background="#FF2F2F2F"
                                        BorderBrush="#FF3F3F3F"
                                        BorderThickness="1"
                                        CornerRadius="6,6,0,0"
                                        Margin="0,0,6,0"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"
                                                      ContentSource="Header"
                                                      RecognizesAccessKey="True"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#FF4C5BD4"/>
                                        <Setter TargetName="Bd" Property="BorderBrush" Value="#FF6F8AFB"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="Bd" Property="Opacity" Value="0.6"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding DisplayName}" FontSize="13"/>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Border Background="#FF262626" BorderBrush="#FF3B3B3B" BorderThickness="1" CornerRadius="6" Padding="14">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Check a field to show it, then edit the header label if desired."
                                       FontSize="12"
                                       Foreground="#FFE4E4E4"/>

                            <ListView Grid.Row="1"
                                      ItemsSource="{Binding Metrics}"
                                      Margin="0,10,0,0"
                                      BorderThickness="1"
                                      BorderBrush="#FF404040"
                                      Background="#FF1F1F1F"
                                      HorizontalContentAlignment="Stretch">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Show" Width="60">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding IsEnabled, Mode=TwoWay}" HorizontalAlignment="Center"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Field" Width="200" DisplayMemberBinding="{Binding Key}"/>
                                    <GridViewColumn Header="Header Text" Width="200">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBox Text="{Binding Header, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                         MinWidth="140"
                                                         Background="#FFEEEEEE"
                                                         Foreground="Black"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                        </Grid>
                    </Border>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>

    <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
        <Button Content="Cancel"
            Width="90"
            Margin="0,0,12,0"
            IsCancel="True"
            Background="#FF2F2F2F"
            Foreground="White"
            BorderBrush="#FF555555"/>
        <Button Content="Save"
            Width="90"
            IsDefault="True"
            Click="Save_Click"
            Background="#FF4C5BD4"
            Foreground="White"
            BorderBrush="#FF6F8AFB"/>
        </StackPanel>
    </Grid>
</Window>
